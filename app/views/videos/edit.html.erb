<div class="title">
	Редактировать видео
</div>

<%= form_for(:video, :url => {:action => 'update', :id => @video.id}, :html => { :multipart => true }) do |f| %>
 
	<%= render(:partial => 'shared/video_form', :locals => {:f => f}) %>
	
	<div class="b">
		<%= submit_tag("Обновить") %>
	</div>

<% end %>