<%= form_for(:news, :url => {:action => 'create', :section => @news.section}) do |f| %>

<div class="b">
	<%= render(:partial => 'shared/news_form', :locals => {:f => f}) %>

	<%= f.select(:section, @sections << @news.section) %> 
</div>

	<%= submit_tag("Добавить") %>
<% end %>

