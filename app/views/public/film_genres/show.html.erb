<% provide(:title, @genre.title) %>

<% if @films.any? %>
	<div class="a">
		<% render partial: 'public/shared/_films', locals: { @films => films } %>
	</div>
	
	<%= will_paginate @films, previous_label: "", next_label: "", class: "b" %>
<% else %>
	<div class="a">
		Пусто
	</div>
<% end %>
