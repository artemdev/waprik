<% provide(:title, @album.name.mb_chars.capitalize) %>

<div class="a">
	<% if @tracks.any? %>
		<% @tracks.each do |track| %>
			<%= link_to track.name, track_path(track.permalink) %> <br />
		<% end %>
	<% end %>
</div>