<% provide(:title, "Добавить сериал") %>

<%= error_messages_for(@serial) %>

<%= form_for(@serial, url: admin_serials_path, html: {multipart: true}) do |f| %>
	
	<%= render(partial: 'admin/shared/serial', locals: {:f => f}) %>

	<%= f.submit "добавить", class: "next" %>
<% end %>