<% provide(:title, @genre.title) %>

<% if @films.any? %>
	<% @films.each do |film| %>
		<%= link_to film.title, admin_film_path(film) %> <br />
	<% end %>
<% else %>
	Пусто
<% end %>

<%= will_paginate @films, next_label: "", previous_label: "" %>