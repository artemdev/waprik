<% provide(:title, "Фильм с кинопоиска") %>

<% if @movies.any? %>
	<% @movies.each do |movie| %>
		<%= content_tag(:div, class: 'kinopoisk-film') do %>
			<%= link_to movie.title, new_admin_film_path(movie_url: movie.url) %>
			<br />
		<% end %>
	<% end %>
<% else %>
	<div class="film-not-found">
		Фильмов не найдено
	</div>
<% end %>