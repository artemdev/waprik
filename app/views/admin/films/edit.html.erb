<% provide(:title, "Редактировать фильм") %>

<%= error_messages_for @film %>

<%= form_for(:film, url: admin_film_path(@film), method: :put, :html => { :multipart => true }) do |f| %>

	<%= render partial: 'admin/shared/film_form', locals: {:f => f} %>

	<%= f.submit "Обновить" %>
<% end %>