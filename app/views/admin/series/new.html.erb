<%= form_for(@serie, url: {action: 'create', serial_id: @serial.id}, html: {multipart: true}) do |f| %>
	<%= error_messages_for(@serie) %>

		<%= render(partial: 'admin/shared/series', locals: {:f => f, :series => @serie}) %>
		
	<%= f.submit("Добавить") %>
<% end %>